(()=>{async function go(){let d;try{d=await fetch('/data/charts.json',{cache:'no-store'}).then(r=>r.json())}catch(e){return}
const fill=(id,a,retro=false)=>{const x=document.getElementById(id);if(!x)return;x.innerHTML=a.map(s=>`<div class="v26400-row"><div class="v26400-rank">${String(s.rank).padStart(2,'0')}</div><div class="v26400-song"><strong>${s.artist} — ${s.title}</strong><small>${retro?(s.year||''):'DJ FOLSOE chart'}</small></div><div class="v26400-edit">${s.tidal_url?`<a href="${s.tidal_url}" target="_blank" rel="noopener">TIDAL ↗</a>`:'EDIT DATA'}</div></div>`).join('')};fill('top20',d.top20);fill('retro10',d.retro_top10,true);
const p=document.getElementById('tidalPlaylists');if(p)p.innerHTML=d.tidal_playlists.map(v=>v.url?`<a class="v26400-playlist" href="${v.url}" target="_blank" rel="noopener"><strong>${v.title}</strong><small>${v.description} · OPEN IN TIDAL ↗</small></a>`:`<div class="v26400-playlist" data-empty="true"><strong>${v.title}</strong><small>${v.description}</small></div>`).join('')}
go()})()