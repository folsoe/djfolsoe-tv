const API_BASE='https://djfolsoe-tv-api.sunefolsoe.workers.dev';let commands=[];const $=s=>document.querySelector(s);
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function render(){$('#adminGrid').innerHTML=commands.map((c,i)=>`<article class="card"><div class="command">${esc(c.command)}</div><input data-i="${i}" data-k="title" value="${esc(c.title)}"><textarea data-i="${i}" data-k="description">${esc(c.description)}</textarea><input data-i="${i}" data-k="usage" value="${esc(c.usage)}"><label><input type="checkbox" data-i="${i}" data-k="active" ${c.active?'checked':''}> Active</label><label><input type="checkbox" data-i="${i}" data-k="public" ${c.public?'checked':''}> Public</label><label><input type="checkbox" data-i="${i}" data-k="overlay" ${c.overlay?'checked':''}> Overlay</label></article>`).join('');$('#status').innerHTML=`<div><strong>${commands.length}</strong><br><span>Commands loaded</span></div>`}
async function load(){const d=await(await fetch(API_BASE+'/api/ext003/commands?all=1',{cache:'no-store'})).json();commands=d.commands||[];render()}
document.addEventListener('input',e=>{const i=e.target.dataset.i,k=e.target.dataset.k;if(i===undefined||!k)return;commands[Number(i)][k]=e.target.type==='checkbox'?e.target.checked:e.target.value});
async function save(){const token=$('#token').value;if(!token)return alert('ADMIN_TOKEN mangler');const d=await(await fetch(API_BASE+'/api/ext003/admin/save',{method:'POST',headers:{'Content-Type':'application/json','X-Admin-Token':token},body:JSON.stringify({commands})})).json();$('#status').innerHTML=`<pre>${esc(JSON.stringify(d,null,2))}</pre>`;if(d.ok)load()}
async function reset(){const token=$('#token').value;if(!token)return alert('ADMIN_TOKEN mangler');const d=await(await fetch(API_BASE+'/api/ext003/admin/reset',{method:'POST',headers:{'Content-Type':'application/json','X-Admin-Token':token},body:'{}'})).json();if(d.ok)load();else alert(JSON.stringify(d))}
$('#reload').addEventListener('click',load);$('#saveAll').addEventListener('click',save);$('#reset').addEventListener('click',reset);load();
