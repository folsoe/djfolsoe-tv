import{esc}from'../cms-dom.js';export async function mount(host,{state}){const news=state.news?.articles||[];host.innerHTML=`<header class="moduleHeader"><div><small>MUSIC STORIES</small><h2>Editorial and external news</h2><p>News management is isolated from the rest of the CMS.</p></div></header><div class="list">${news.slice(0,60).map(a=>`<article class="listRow"><span>▤</span><div><strong>${esc(a.title)}</strong><small>${esc(a.sourceName||'DJ FOLSOE')} · ${esc(a.featured?'Featured':'Standard')}</small></div></article>`).join('')||'<div class="cmsCard">No stories loaded.</div>'}</div>`}