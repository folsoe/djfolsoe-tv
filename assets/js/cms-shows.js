import{request}from'./cms-api.js';import{toast}from'./cms-ui.js';import{esc}from'./cms-dom.js';export async function mount(host,{state,token}){let shows=structuredClone(state.core?.featuredShows||[]);const render=()=>{host.innerHTML=`<header class="moduleHeader"><div><small>PROGRAMMES</small><h2>Edit your shows</h2><p>Titles, times, descriptions and themes.</p></div><div class="moduleActions"><button id="addShow" class="secondary">Add show</button><button id="saveShows">Save shows</button></div></header><div class="cmsForm">${shows.map((s,i)=>`<article class="cmsCard" data-i="${i}"><h3>${esc(s.title||`Show ${i+1}`)}</h3><div class="grid2"><label class="cmsField">Title<input data-f="title" value="${esc(s.title||'')}"></label><label class="cmsField">When<input data-f="time" value="${esc(s.time||'')}"></label></div><label class="cmsField">Description<textarea data-f="description">${esc(s.description||'')}</textarea></label><label class="cmsField">Theme<input data-f="theme" value="${esc(s.theme||'weekend')}"></label></article>`).join('')}</div>`;host.querySelector('#addShow').onclick=()=>{shows.push({title:'New show',time:'Special',description:'',theme:'weekend'});render()};host.querySelector('#saveShows').onclick=async()=>{host.querySelectorAll('[data-i]').forEach(card=>{const s=shows[Number(card.dataset.i)];card.querySelectorAll('[data-f]').forEach(f=>s[f.dataset.f]=f.value)});try{const r=await request('/api/cms/admin/shows',{token,method:'POST',body:{shows}});state.core=r.core;toast('Shows saved.')}catch(e){toast(e.message,true)}}};render()}