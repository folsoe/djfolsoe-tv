import{request}from'./cms-api.js';import{toast}from'./cms-ui.js';import{esc}from'./cms-dom.js';export async function mount(host,{state,token}){const mods=state.modules||[];host.innerHTML=`<header class="moduleHeader"><div><small>CONTENT BLOCKS</small><h2>Published, draft and scheduled content</h2><p>Open, publish, hide or duplicate blocks without touching code.</p></div></header><div class="list">${mods.map(m=>`<article class="listRow"><span>${esc((m.type||'B')[0].toUpperCase())}</span><div><strong>${esc(m.title)}</strong><small>${esc(m.type)} · ${esc(m.status)} · ${esc(m.theme)}</small></div><div class="rowActions"><button data-toggle="${esc(m.id)}" class="secondary">${m.status==='published'?'Unpublish':'Publish'}</button><button data-duplicate="${esc(m.id)}" class="secondary">Duplicate</button></div></article>`).join('')||'<div class="cmsCard">No content blocks yet.</div>'}</div>`;host.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{const m=mods.find(x=>x.id===b.dataset.toggle);try{const r=await request('/api/cms/admin/module/toggle',{token,method:'POST',body:{id:m.id,status:m.status==='published'?'draft':'published'}});Object.assign(m,r.module);toast('Content updated.');mount(host,{state,token})}catch(e){toast(e.message,true)}});host.querySelectorAll('[data-duplicate]').forEach(b=>b.onclick=async()=>{try{const r=await request('/api/cms/admin/module/duplicate',{token,method:'POST',body:{id:b.dataset.duplicate}});mods.unshift(r.module);toast('Draft copy created.');mount(host,{state,token})}catch(e){toast(e.message,true)}})}