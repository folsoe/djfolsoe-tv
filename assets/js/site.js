let DATA, currentProgram, ticker=0, LANG="da";
const $=id=>document.getElementById(id);
const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
async function boot(){
  LANG=localStorage.getItem("djf_lang")||"da";
  const res=await fetch("assets/data/cms.json?cb="+Date.now());
  DATA=await res.json();
  currentProgram=findCurrentOrNext();
  setupLanguage(); applyLanguage(); applyProgram(currentProgram.program,currentProgram.isLive);
  renderPrograms(); renderRequests(); renderChart(); renderNews(); setupRequestForm();
  setInterval(updateTicker,3500);
}
function t(key){return DATA.ui?.[LANG]?.[key]||DATA.ui?.da?.[key]||key;}
function pi18n(p,key){return p.i18n?.[LANG]?.[key]||p.i18n?.da?.[key]||p[key]||"";}
function stationText(key){return DATA.station?.i18n?.[LANG]?.[key]||DATA.station?.i18n?.da?.[key]||DATA.station?.[key]||"";}
function setupLanguage(){document.querySelectorAll(".languageSwitch button").forEach(btn=>{btn.onclick=()=>{LANG=btn.dataset.lang;localStorage.setItem("djf_lang",LANG);applyLanguage();applyProgram(currentProgram.program,currentProgram.isLive);renderPrograms();renderRequests();renderNews();};});}
function applyLanguage(){document.documentElement.lang=LANG;document.querySelectorAll("[data-i18n]").forEach(el=>el.textContent=t(el.dataset.i18n));$("reqName").placeholder=t("namePlaceholder");$("reqSong").placeholder=t("songPlaceholder");document.querySelectorAll(".languageSwitch button").forEach(btn=>btn.classList.toggle("active",btn.dataset.lang===LANG));}
function rgb(hex){hex=String(hex||"#ffd166").replace("#","");const n=parseInt(hex,16);return `${(n>>16)&255},${(n>>8)&255},${n&255}`;}
function nextDateFor(day,time){const now=new Date(),target=days.indexOf(day);let d=new Date(now);d.setDate(now.getDate()+((target-now.getDay()+7)%7));const [h,m]=String(time).split(":").map(Number);d.setHours(h||0,m||0,0,0);if(d<now)d.setDate(d.getDate()+7);return d;}
function findCurrentOrNext(){const now=new Date();for(const p of DATA.programs||[]){const start=nextDateFor(p.day,p.time);if(start>now)start.setDate(start.getDate()-7);const end=new Date(start.getTime()+(p.duration||120)*60000);if(now>=start&&now<=end)return{program:p,isLive:true,nextDate:start};}return (DATA.programs||[]).map(p=>({program:p,isLive:false,nextDate:nextDateFor(p.day,p.time)})).sort((a,b)=>a.nextDate-b.nextDate)[0]||{program:{},isLive:false,nextDate:new Date()};}
function applyProgram(p,isLive){document.documentElement.style.setProperty("--a",p.colorA||"#ffd166");document.documentElement.style.setProperty("--b",p.colorB||"#00d4ff");document.documentElement.style.setProperty("--rgb",rgb(p.colorA||"#ffd166"));$("heroTitle").textContent=isLive?`${t("liveNow")} · ${pi18n(p,"title")}`:`${t("next")} · ${pi18n(p,"title")}`;$("heroDesc").textContent=stationText("description");$("liveStatus").textContent=isLive?t("liveNow"):t("offline");$("viewers").textContent=DATA.station?.viewers??0;$("nowNext").textContent=isLive?t("liveNow"):t("next");$("programIcon").textContent=p.icon||"📺";$("programTitle").textContent=pi18n(p,"title");$("programDesc").textContent=pi18n(p,"description");$("tickerBadge").textContent=pi18n(p,"type").toUpperCase();$("nextTitle").textContent=pi18n(p,"title");$("nextTime").textContent=currentProgram.nextDate.toLocaleString(LANG==="da"?"da-DK":LANG==="de"?"de-DE":"en-US",{weekday:"long",day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"});$("nextType").textContent=pi18n(p,"type");$("nextFocus").textContent=pi18n(p,"musicFocus");$("nextDescription").textContent=pi18n(p,"description");}
function renderPrograms(){$("programGrid").innerHTML=(DATA.programs||[]).map(p=>`<article class="programCard" style="--pa:${p.colorA};--pb:${p.colorB}"><div style="font-size:34px">${p.icon||"📺"}</div><b>${pi18n(p,"title")}</b><p>${p.day} · ${p.time}</p><p>${pi18n(p,"description")}</p><small>${pi18n(p,"musicFocus")}</small></article>`).join("");$("reqShow").innerHTML=(DATA.programs||[]).map(p=>`<option value="${pi18n(p,"title")}">${pi18n(p,"title")}</option>`).join("");}
function renderChart(){$("chartList").innerHTML=(DATA.chart||[]).map(t=>`<div class="chartRow"><div class="pos">${t.pos}</div><div><b>${t.artist} - ${t.title}</b><br><small>${t.status||""}</small></div><div>${t.genre||""}</div></div>`).join("");}
function newsLines(){const key=currentProgram.program.id;return DATA.newsI18n?.[key]?.[LANG]||DATA.newsI18n?.[key]?.da||DATA.news?.[key]||[];}
function renderNews(){const arr=newsLines();$("newsGrid").innerHTML=arr.map((n,i)=>`<article class="newsItem"><b>${i?"PROGRAM RADAR":"TOP STORY"}</b><p>${n}</p></article>`).join("");}
function updateTicker(){const arr=newsLines();$("tickerText").textContent=(arr.length?arr:["DJ FOLSOE TV · Music Television From Denmark"])[ticker++%(arr.length||1)];}
function setupRequestForm(){$("requestForm").addEventListener("submit",e=>{e.preventDefault();const item={name:$("reqName").value||"Viewer",song:$("reqSong").value||"Artist - Song",show:$("reqShow").value,when:$("reqWhen").value||t("next"),lang:LANG};const all=JSON.parse(localStorage.getItem("djf_requests")||"[]");all.unshift(item);localStorage.setItem("djf_requests",JSON.stringify(all.slice(0,30)));renderRequests();e.target.reset();applyLanguage();});}
function renderRequests(){const all=JSON.parse(localStorage.getItem("djf_requests")||"[]");$("requestOutput").innerHTML=all.length?all.map(r=>`<div class="requestItem"><b>${r.song}</b><br><small>${r.name} · ${r.show} · ${r.when}</small></div>`).join(""):`<p class='muted'>${t("noRequests")}</p>`;}
document.addEventListener("DOMContentLoaded",boot);